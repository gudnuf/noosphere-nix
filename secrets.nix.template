# Template for secrets.nix
# Copy this to secrets.nix and fill in your actual values
# secrets.nix is gitignored and will never be committed

{
  # GitHub Personal Access Token for Claude Code MCP
  # Get one from: https://github.com/settings/tokens/new
  # Required scopes: repo, read:user, read:org
  GITHUB_PERSONAL_ACCESS_TOKEN = "ghp_your_token_here";

  # Add other secrets as needed
  # EXAMPLE_API_KEY = "your_key_here";
}
